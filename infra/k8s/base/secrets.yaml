# ⚠️  TEMPLATE ONLY — DO NOT commit real values.
# Populate via: kubectl create secret generic wavestack-secrets --from-env-file=.env.prod
# Or use an external secrets operator (e.g. External Secrets Operator + AWS Secrets Manager).
apiVersion: v1
kind: Secret
metadata:
  name: wavestack-secrets
  namespace: wavestack
type: Opaque
stringData:
  DATABASE_URL: "postgresql://CHANGE_ME:CHANGE_ME@postgres:5432/wave"
  REDIS_URL: "redis://:CHANGE_ME@redis:6379"
  JWT_PRIVATE_KEY: "CHANGE_ME"
  JWT_PUBLIC_KEY: "CHANGE_ME"
  NEXTAUTH_SECRET: "CHANGE_ME"
  INTERNAL_SERVICE_SECRET: "CHANGE_ME"
  TWITCH_CLIENT_ID: "CHANGE_ME"
  TWITCH_CLIENT_SECRET: "CHANGE_ME"
  DISCORD_TOKEN: "CHANGE_ME"
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
